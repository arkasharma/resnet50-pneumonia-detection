# Note: the script is a Colab export and contains Colab-specific commands (drive.mount, shell !pip calls).
# If running locally, remove or adapt Colab-specific lines in the script.

numpy>=1.21
pandas>=1.3
matplotlib>=3.4
Pillow>=9.0
seaborn>=0.11
scikit-learn>=1.0
tensorflow>=2.10
tensorflow-datasets>=4.8
tensorflow-addons>=0.18
tqdm>=4.60
opencv-python>=4.5

# Optional / Colab-only
# google-colab    # Only available on Colab; not required when running locally
